// Starts a thread for each resource type and loads it
void res_load(void);

// Loads tiles in a thread and writes them to the registry
void tiles_load(void);

// Loads items in a thread writes them to the registry
void items_load(void);
